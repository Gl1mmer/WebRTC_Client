import 'package:flutter/material.dart';

class TeamPageView extends StatelessWidget {
  const TeamPageView({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Team'));
}
