import 'package:flutter/material.dart';

class AutoResponderPage extends StatelessWidget {
  const AutoResponderPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Will be added later'));
}
