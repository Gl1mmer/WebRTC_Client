import 'package:flutter/material.dart';

class FeaturedPageView extends StatelessWidget {
  const FeaturedPageView({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Featured'));
}
