import 'package:flutter/material.dart';

class CounterpartiesPageView extends StatelessWidget {
  const CounterpartiesPageView({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Will be added later'));
}
